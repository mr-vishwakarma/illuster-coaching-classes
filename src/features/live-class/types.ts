// ─── LIVE CLASS TYPES ─────────────────────────────────────────────────────────
export interface UpcomingClass {
  id: string;
  courseId: string;
  title: string;
  faculty: string;
  date: string;
  time: string;
  platform: string;
  liveLink: string;
}
